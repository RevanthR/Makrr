# Postgres (Neon or any PostgreSQL). Get from Neon.tech or Vercel Marketplace.
POSTGRES_URL=postgresql://user:password@host/database?sslmode=require

# Vercel Blob. Create a store in Vercel project → Storage → Blob; token is auto-added.
BLOB_READ_WRITE_TOKEN=

# Admin dashboard password (plain text; compare in lib/auth.ts).
ADMIN_PASSWORD=your-secure-password

# Session cookie value; must be a long random string. Generate with: openssl rand -hex 32
ADMIN_SESSION_SECRET=
